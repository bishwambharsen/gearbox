// STUB — Workstream 3 replaces the body; keep the exported signature.
import type { GearboxConfig, Ledger } from '../types.js';

export function createLedger(_config: GearboxConfig): Ledger {
  return {
    record() {},
  };
}
